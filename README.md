# WorldAgen project page

Source for [worldagen.github.io](https://worldagen.github.io). Built with [Astro](https://astro.build).

```bash
npm install
npm run dev      # local dev server
npm run build    # static build to ./dist
```

Deployed automatically to GitHub Pages on push to `main`.
